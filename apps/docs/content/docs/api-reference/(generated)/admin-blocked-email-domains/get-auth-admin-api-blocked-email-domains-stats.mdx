---
title: 'GET /auth/admin/api/blocked-email-domains/stats'
description: 'Blocklist stats (current count + size of the built-in default list)'
---

import { EndpointDocs } from '@/components/endpoint-docs';

# Blocklist stats (current count + size of the built-in default list)

<EndpointDocs method="get" path="/auth/admin/api/blocked-email-domains/stats" />
