---
title: 'DELETE /auth/admin/api/users/{id}/sessions/{sessionId}'
description: 'Revoke a single user session'
---

import { EndpointDocs } from '@/components/endpoint-docs';

# Revoke a single user session

<EndpointDocs method="delete" path="/auth/admin/api/users/{id}/sessions/{sessionId}" />
