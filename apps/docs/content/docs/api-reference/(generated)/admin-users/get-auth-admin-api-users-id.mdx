---
title: 'GET /auth/admin/api/users/{id}'
description: 'Get a user (with roles, sessions, identities)'
---

import { EndpointDocs } from '@/components/endpoint-docs';

# Get a user (with roles, sessions, identities)

<EndpointDocs method="get" path="/auth/admin/api/users/{id}" />
