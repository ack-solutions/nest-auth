---
title: 'DELETE /auth/admin/api/users/{id}/totp-devices/{deviceId}'
description: 'Remove a user''s TOTP device'
---

import { EndpointDocs } from '@/components/endpoint-docs';

# Remove a user's TOTP device

<EndpointDocs method="delete" path="/auth/admin/api/users/{id}/totp-devices/{deviceId}" />
