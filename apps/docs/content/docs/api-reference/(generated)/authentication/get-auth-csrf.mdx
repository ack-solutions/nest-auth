---
title: 'GET /auth/csrf'
description: 'Cookie mode: sets/rotates the double-submit CSRF cookie and returns the token to echo in the CSRF header (default `x-csrf-token`) on state-changing requests. Call with credentials so the cookie is ...'
---

import { EndpointDocs } from '@/components/endpoint-docs';

# Get a CSRF token

<EndpointDocs method="get" path="/auth/csrf" />
